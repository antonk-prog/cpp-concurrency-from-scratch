// Лаба 3.1. Напиши класс Counter с нуля.
// Требования — в task.md. Здесь каркас, который компилируется.
#include <mutex>
#include <thread>

class Counter {
    // Напиши: поле value_ и мьютекс m_, методы increment() и get() const.
};

int main() {
    Counter counter;
    (void)counter;
    // Напиши: 4 потока по 100000 инкрементов, join() всех, вывод get().
    return 0;
}
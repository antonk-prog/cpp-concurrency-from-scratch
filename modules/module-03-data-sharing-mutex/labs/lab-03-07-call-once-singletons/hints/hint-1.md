# Направление мысли

Гонка возникает на участке «проверил, что ещё не создано → создал». Нужен
механизм, который сам отвечает за «создать ровно один раз». В модуле 3 таких
механизма два: для произвольного кода инициализации и для локальной переменной.

Подумай: что делает `std::call_once` вместе с `std::once_flag`, и что стандарт
гарантирует про инициализацию `static`-локальной переменной?
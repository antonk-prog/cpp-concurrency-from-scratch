// Лаба 3.7. Напиши два потокобезопасных синглтона с нуля.
// Требования — в task.md. Здесь каркас, который компилируется.
#include <memory>
#include <mutex>

class Singleton {
    // Напиши: конструктор, увеличивающий static int instances.
};

// Вариант 1: геттер на std::call_once + std::once_flag
// Вариант 2: геттер на static-локальной переменной
// (объяви здесь оба геттера)

int main() {
    // Напиши: эталоны обоих синглтонов, 8 потоков по 1000 обращений,
    // проверку одинаковости адресов, после join() — instances == 2,
    // вывод "All checks passed".
    return 0;
}
# Лаба 3.6. Слияние результатов с отложенной блокировкой — средняя
Модуль: 3. Совместное использование данных несколькими потоками
Тип: допиши TODO

## Мотивация

Поток часто работает так: что-то посчитал локально (без общего состояния),
затем коротко обновил общие данные и снова продолжил локальную работу.
Держать мьютекс всю локальную фазу — значит зря блокировать других.
`std::unique_lock` с флагом `std::defer_lock` позволяет создать объект
блокировки заранее, а взять её только на время слияния.

## Задача

В стартовом коде несколько воркеров копят локальные суммы и сливают их
в общий `shared_total`. Допиши три места (TODO):

1. Создай `std::unique_lock<std::mutex> lk(m, std::defer_lock)` **до** локальной
   работы — мьютекс при этом ещё не заблокирован.
2. После локальной работы вызови `lk.lock()`, прибавь `local` к `shared_total`
   и сразу `lk.unlock()` — блокировка держится только на время слияния.
3. В `main` запусти `kWorkers = 4` потоков, дождись всех через `join()`,
   выведи `shared_total`.

## Требования

1. Сборка без ошибок и предупреждений:
   `g++ -std=c++17 -pthread -Wall -Wextra main.cpp -o /tmp/lab_03_06`.
2. `unique_lock` создаётся с `std::defer_lock`; блокировка берётся и снимается
   явными `lock()`/`unlock()` через объект `unique_lock`.
3. Никаких `lock_guard` в воркере; никаких `sleep`.
4. Ровно `kWorkers = 4` потока по `kIterations = 2000` итераций, все
   `join()`-ятся.
5. Вывод ровно `Shared total: 998000000`.

## Стартовый код

Файл `starter/main.cpp` (компилируется, но без блокировки итог неверен).
Собери:

```
g++ -std=c++17 -pthread -Wall -Wextra main.cpp -o /tmp/lab_03_06
```

## Чеклист самопроверки

- [ ] Компилируется с `-Wall -Wextra` без замечаний.
- [ ] В воркере используется `unique_lock` с `defer_lock` и явные
      `lock()`/`unlock()`.
- [ ] Вывод всегда `Shared total: 998000000`, независимо от запуска.
- [ ] Прогнал под ThreadSanitizer — гонок нет.

## Подсказки и решение

Застрял — смотри подсказки по порядку: `hints/hint-1.md` (направление мысли),
затем `hints/hint-2.md` (механизм). Готовое решение — в `solution/solution.cpp`
и `solution/solution.md`. Грабли — в `pitfalls.md`.